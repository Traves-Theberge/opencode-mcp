# OpenCode Server Configuration
# OpenCode server URL
OPENCODE_SERVER_URL=http://localhost:4096

# Auto-start OpenCode server if not running
OPENCODE_AUTO_START=true

# Default model to use (provider/model format)
# OPENCODE_DEFAULT_MODEL=anthropic/claude-sonnet-4

# Request timeout in milliseconds
OPENCODE_TIMEOUT=120000

# MCP Transport Mode
# Options: stdio, http
MCP_TRANSPORT=stdio

# HTTP Port (only used when MCP_TRANSPORT=http)
MCP_HTTP_PORT=3000

# CORS Origins (comma-separated, only used when MCP_TRANSPORT=http)
# MCP_CORS_ORIGINS=*
# MCP_CORS_ORIGINS=http://localhost:5173,https://app.example.com
