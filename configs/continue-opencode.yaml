name: OpenCode MCP Server
version: 0.1.0
schema: v1
mcpServers:
  - name: opencode
    command: npx
    args:
      - "-y"
      - "@opencode-mcp/server"
    env:
      OPENCODE_SERVER_URL: "http://localhost:4096"
